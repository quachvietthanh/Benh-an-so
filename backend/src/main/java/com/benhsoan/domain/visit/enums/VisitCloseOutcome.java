package com.benhsoan.domain.visit.enums;

/**
 * The two close outcomes supported by NCL-04-CN-009.
 * The outcome identifies the type of closure; the persisted reason explains why.
 */
public enum VisitCloseOutcome {
    EARLY_ENDED,
    CANCELLED
}
